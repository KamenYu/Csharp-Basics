using System;
namespace recap
{
    class Program
    {
        static void Main(string[] args)
        {
            // to convert string into int, double, char, bool with PARSE method
            // to convert any onther variable into any other variable use Casting

            //double a = 5.5;
            //int b = a; // impossible because a is double, be is integer, but
            //int c = (int)a; // the casting method is inside the parenthesis




            //for (int i = 1; i <=3;)    // without giving the steps (i++ or other) => infinite loop
            //{
            //    Console.WriteLine(i);
            //}

            //for (; ; )
            //{
            //    Console.WriteLine("SoftUni"); // infinite loop
            //}

            //for (int i = 0; i < 2; i += 0.5) // you cannot give a double value to an integer, thus  comile time error
            //{
            //    Console.WriteLine(i + ", ");
            //}


            //while (true)
            //{
            //    Console.WriteLine("Infinite Loop"); // without a "break" the loop is infinite
            //}

            //int a = 5;
            //while (true)
            //{
            //    if (a > 10)
            //    {
            //        break;
            //    }                                  // while a < 10 type a = a, with a ++, when a>10 break
            //    Console.WriteLine("a = " + a);
            //    a++;
            //}

            //while (true)
            //{
            //    string input = Console.ReadLine();
            //    if (input == "Stop")
            //    {                                           // until Stop is written, "cw" will be executed
            //        break;
            //    }

            //    Console.WriteLine("Infinite loop");
            //}

            //int a = 5;

            //while (a <= 10)
            //{
            //    Console.WriteLine("a = " + a);         //breaking the loop without the "break" statement
            //    a++;
            //}
        }
    }
}
